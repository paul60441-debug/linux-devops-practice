name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code
        run: |
          cd ~/docker/day17
          git pull

      - name: Restart containers
        run: |
          cd ~/docker/day17
          docker compose down
          docker compose up -d
